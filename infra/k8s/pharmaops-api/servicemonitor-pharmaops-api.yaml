apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: pharmaops-api
  namespace: monitoring
  labels:
    release: monitoring
spec:
  namespaceSelector:
    matchNames:
      - pharmaops
  selector:
    matchLabels:
      app: pharmaops-api
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 15s
